---
title: "Vibe v2.4.76: 18K 다운로드와 베타의 끝이 보인다"
date: "2026-01-19"
category: "tech"
description: "2개월 만에 18K 다운로드. 반복적 SPEC 리뷰, Large SPEC 자동 분할, OS 언어 감지. 버전 넘버링 실수와 실질적 베타 상태 고백"
tags: ["vibe", "ai-coding", "claude-code", "spec-review", "18k-downloads", "beta", "release", "v2.4"]
author: "Su"
lang: "ko"
---

# Vibe v2.4.76: 18K 다운로드와 베타의 끝이 보인다

## 버전 넘버링에 대한 고백

솔직히 말하면, **버전 넘버를 너무 쉽게 생각했습니다.**

```
2025-11-17: v0.1.0 (첫 배포)
2026-01-07: v1.0.0 (2개월 만에 메이저)
2026-01-10: v2.0.0 (3일 만에 메이저)
2026-01-19: v2.4.76 (...)
```

지금 v2.4.76이지만, **실제로는 0.x.x 베타 상태**입니다.

기능을 추가하고 버그를 수정할 때마다 버전을 올렸더니 이렇게 됐습니다. SemVer 원칙대로라면 아직 v0.4.76 정도가 맞습니다.

**하지만 숫자가 중요한 게 아닙니다.** 중요한 건 실제로 동작하느냐입니다.

---

## 18K 다운로드

2개월 만에 npm 다운로드 **18,000건**을 달성했습니다.

```
11월 (첫 배포): 769
12월: 50 (조용한 기간)
1월 6일 이후: 🚀 폭발
  - 일평균 ~900/일
  - 피크: 2,382/일 (1월 15일)
```

1월 초 어딘가에서 입소문이 난 것 같습니다. 정확히 어디인지는 모르겠지만, 감사합니다.

---

## v2.4 이후 주요 변경사항

v2.4 Agent Orchestrator 이후 많은 개선이 있었습니다.

### 1. 반복적 SPEC 리뷰 (Convergence Loop)

SPEC 품질을 높이기 위해 **최대 3라운드 반복 리뷰**를 도입했습니다.

```
SPEC 초안
    ↓
┌─────────────────────────────────────────┐
│  Round 1: GPT + Gemini 병렬 리뷰        │
│           ↓                             │
│  피드백 적용 → SPEC 업데이트            │
│           ↓                             │
│  Round 2: 업데이트된 SPEC 재리뷰        │
│           ↓                             │
│  피드백 적용 → SPEC 업데이트            │
│           ↓                             │
│  Round 3: 최종 리뷰 (필요시)            │
└─────────────────────────────────────────┘
    ↓
수렴 완료
```

**수렴 조건:**
- GPT와 Gemini 모두 P1/P2 이슈 없음
- 이전 라운드와 피드백 동일 (새 개선점 없음)
- 최대 3라운드 도달

빠른 실행이 필요하면 `--quick` 옵션:

```bash
/vibe.spec "feature" --quick  # 단일 리뷰
/vibe.spec "feature"          # 반복 리뷰 (기본값)
```

### 2. Large SPEC 자동 분할

5개 이상 Phase나 15개 이상 파일을 생성하는 대규모 SPEC은 **자동으로 Phase별 분할**됩니다.

```
.claude/vibe/specs/{feature}/
├── _index.md           # Master SPEC
├── phase-1-setup.md
├── phase-2-core.md
└── phase-3-ui.md

.claude/vibe/features/{feature}/
├── _index.feature      # Master Feature
├── phase-1-setup.feature
├── phase-2-core.feature
└── phase-3-ui.feature
```

**핵심 규칙: 모든 SPEC 파일은 대응하는 Feature 파일이 있어야 합니다.**

BDD 검증이 누락되는 것을 방지합니다.

### 3. Dual Auth (OAuth + API Key)

Gemini/GPT 호출 시 **OAuth가 실패하면 자동으로 API Key로 폴백**합니다.

```
OAuth 시도 → Rate limit/만료 → API Key 폴백 → 성공
```

사용자는 중단 없이 작업을 계속할 수 있습니다.

### 4. OS 언어 자동 감지

`vibe init` 또는 `vibe update` 실행 시 OS 언어를 감지하여 응답 언어를 설정합니다.

- 한국어 OS → Claude가 한국어로 응답
- 영어 OS → Claude가 영어로 응답

더 이상 매번 "한국어로 답해줘"라고 할 필요 없습니다.

### 5. 크로스플랫폼 경로 해결

Windows, macOS, Linux 모두에서 안정적으로 동작합니다.

```javascript
// Windows: %APPDATA%/vibe/
// macOS/Linux: ~/.config/vibe/
const VIBE_PATH = process.env.APPDATA
  || path.join(os.homedir(), '.config');
```

Hook 스크립트, API 키 저장, 모듈 해결 모두 크로스플랫폼으로 동작합니다.

---

## 아키텍처 변화

### MCP → Hook 기반

v2.0에서 MCP 오버헤드를 제거했다고 했는데, v2.4.x에서 **완전히 Hook 기반으로 전환**했습니다.

| 항목 | MCP 방식 | Hook 방식 |
|------|----------|-----------|
| 프로세스 | 별도 MCP 서버 | 단일 프로세스 |
| 통신 | JSON-RPC | 직접 API 호출 |
| 복잡성 | 등록/해제 필요 | 훅 스크립트만 |
| 안정성 | 프로세스 크래시 위험 | 단순 |

### Hook 스크립트 모듈화

```
~/.config/vibe/hooks/scripts/  (또는 %APPDATA%/vibe/hooks/scripts/)
├── utils.js              # 공유 유틸리티
├── session-start.js      # 세션 시작
├── code-check.js         # 코드 체크
├── llm-orchestrate.js    # LLM 오케스트레이션
├── code-review.js        # 코드 리뷰
├── complexity.js         # 복잡도 분석
└── recall.js             # 메모리 회상
```

---

## 현재 상태와 앞으로

### 현재 (베타)

- **동작함**: 핵심 워크플로우 (`/vibe.spec` → `/vibe.run`)
- **안정화 중**: 에지 케이스, 크로스플랫폼 이슈
- **실험 중**: 자기 학습 기능 (제거됨), Graph 저장소 (제거됨)

복잡성 대비 효용이 낮은 기능은 과감히 제거하고 있습니다.

### 앞으로

1. **안정화**: 버그 수정, 에지 케이스 처리
2. **문서화**: 사용법, 튜토리얼, 예제
3. **Vibe IDE**: VSCode Extension Host 기반 전용 IDE (계획 중)

### 버전 정책 변경

v3.0.0부터는 SemVer를 제대로 따를 예정입니다:
- MAJOR: 호환성 깨지는 변경
- MINOR: 새 기능
- PATCH: 버그 수정

지금의 v2.4.76은 사실상 v0.4.76입니다. 1.0.0을 향해 안정화 중입니다.

---

## 업데이트 방법

### 신규 설치

```bash
npm install -g @su-record/vibe
vibe init
```

### 기존 사용자

```bash
npm update -g @su-record/vibe
vibe update
```

---

## 변경 요약

### v2.4.0 → v2.4.76

| 항목 | v2.4.0 | v2.4.76 |
|------|--------|---------|
| SPEC 리뷰 | 단일 | 반복 (max 3) |
| Large SPEC | 수동 분할 | 자동 분할 |
| Auth | OAuth만 | OAuth + API Key |
| 응답 언어 | 수동 설정 | OS 언어 감지 |
| 플랫폼 | 부분 지원 | 크로스플랫폼 |
| 다운로드 | ~5K | **~18K** |

---

## 마무리

2개월 만에 18K 다운로드.

버전 넘버는 엉망이지만, 기능은 동작합니다.

v2.4.76이라고 적혀 있어도 실제로는 베타입니다. 하지만 그 베타의 끝이 보입니다.

**안정화 → 문서화 → 1.0.0 (진짜)**

SPEC 주도 AI 코딩, 2단계 워크플로우.

```bash
/vibe.spec "your-feature"
/vibe.run "your-feature" ultrawork
```

이것만 기억하면 됩니다.

---

> 이전 편: [Vibe v2.4: Agent Orchestrator와 Claude Agent SDK](tech-vibe-09.md)

**GitHub**: https://github.com/su-record/vibe
**NPM**: https://www.npmjs.com/package/@su-record/vibe
